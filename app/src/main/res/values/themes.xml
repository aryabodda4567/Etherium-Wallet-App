<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CryptPay." parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.CryptPay" parent="Theme.AppCompat.DayNight.DarkActionBar">

        <item name="android:statusBarColor">@color/appThemeColor</item>
        <item name="fontFamily">@font/roboto_medium</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:windowBackground">@color/black</item>
        <item name="android:windowEnterAnimation">@android:anim/fade_in</item>
        <item name="android:windowExitAnimation">@android:anim/fade_out</item>
        <item name="colorPrimary">@color/appThemeColor</item>
        <item name="android:navigationBarColor">@color/appThemeColor</item>
    </style>


    <style name="Theme.login" parent="Theme.CryptPay">
        <item name="statusBarBackground">@color/appThemeColor</item>
        <item name="colorPrimary">@color/appThemeColor</item>
        <item name="android:navigationBarColor">@color/appThemeColor</item>
    </style>

    <style name="Theme.splash" parent="Theme.CryptPay">
        <item name="hideNavigationIcon">true</item>
    </style>
    <!-- Base application theme. -->
    <style name="Base.Theme.CryptPay" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>


</resources>